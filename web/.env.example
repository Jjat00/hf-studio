# URL de la API de HF Studio (hf-studio serve) y clave de cliente para la UI.
# Crea la clave con: uv run hf-studio create-key web-ui   (solo vive en el servidor de Next)
HF_STUDIO_URL=http://127.0.0.1:8787
HF_STUDIO_TOKEN=
