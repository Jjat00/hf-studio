# HF Studio · UI web

Interfaz Next.js 16 de HF Studio. La puesta en marcha, la configuración (`web/.env.local`) y la arquitectura están en
el [README principal](../README.md).

```bash
pnpm install
pnpm dev     # http://localhost:3000 (necesita la API en :8787)
```
