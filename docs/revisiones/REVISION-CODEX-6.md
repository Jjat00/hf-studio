# Revisión adversarial de `4e5b99e`

Fecha: 2026-09-24. Alcance: interfaz `web/`, cambio ES/EN, datos del catálogo y flujos de generación. Severidades: media = afecta una función o deja una parte importante de la interfaz en otro idioma; baja = defecto visible de presentación o redacción.

## Hallazgos

1. **[Media] La búsqueda de modelos no encuentra el texto traducido que muestra la interfaz.** `web/src/app/models/page.tsx:42-46` y `web/src/components/studio/model-picker.tsx:55-58,107-109`. Ambas búsquedas comparan solo `id` y `title` originales. Las tarjetas y el selector muestran `workflowLabel(..., "es")`, pero buscar «texto a video» no coincide con «Text to video» ni con `text-to-video`. **Reproducir:** dejar la cookie sin definir (español), abrir `/models`, escribir `texto a video`: desaparecen los modelos cuya tarjeta dice «Texto a video». Sucede también al abrir el selector de modelo desde `/video` y buscar esa frase. Falta indexar el texto mostrado además del identificador original.

2. **[Media] El idioma de la cookie y el proveedor pueden quedar desincronizados entre pestañas.** `web/src/components/i18n-provider.tsx:12-20` y `web/src/app/layout.tsx:17-24`. `useState(initial)` solo usa la prop en el primer montaje; `setLocale` actualiza el estado de la pestaña que recibió el clic. En Next 16, los layouts se conservan durante la navegación y `router.refresh()` conserva el estado React del cliente. **Reproducir:** abrir `/mcp` en dos pestañas con `hfs-lang=es`; en la primera cambiar a EN; en la segunda navegar a otra página sin recarga completa. La cookie ya dice `en`, pero el proveedor, la barra y `html lang` de esa pestaña siguen en `es`, mientras que el contenido de servidor obtenido con `getDict()` puede llegar en inglés. También ocurre si otro código modifica la cookie y se llama `router.refresh()`. Falta una vía para sincronizar el estado del proveedor con el idioma que llega del servidor o con cambios de cookie externos a ese botón.

3. **[Media] El catálogo deja texto inglés dentro del formulario y de «Cómo funciona».** `web/src/components/studio/empty-state.tsx:27-33`, `web/src/components/studio/controls.tsx:35,119-148` y `web/src/lib/schema.ts:89-91`. Se imprimen `detail.notes`, descripciones de campos y nombres no incluidos en `t.controls.fields` sin traducción. **Reproducir:** elegir `alibaba/qwen-image-3/edit`, abrir «Cómo funciona» y leer «Provide 1–3 ordered reference image URLs…»; abrir «Avanzado» y ver `Prompt extend` con la ayuda «Improve the prompt before generation…». Es contenido de la UI, aunque provenga de la API. Falta una presentación localizada para estas notas y campos, con un tratamiento explícito de los términos técnicos que deban conservarse.

4. **[Baja] La lista de modelos muestra códigos de capacidades en inglés.** `web/src/app/models/page.tsx:95-100`. El diccionario ya tiene nombres españoles en `t.models.caps`, pero las etiquetas de cada tarjeta imprimen `c` (`text-to-video`, `image-to-video`, `video-edit`, etc.). **Reproducir:** abrir `/models` en español y mirar las etiquetas bajo cualquier modelo de video. Falta usar etiquetas traducidas para la presentación y conservar los códigos solo para el filtrado/API.

5. **[Baja] La explicación ampliada del costo sigue en inglés.** `web/src/components/studio/cost-panel.tsx:83-86`. `costBasis()` y `costMissing()` traducen lo que se ve en el panel, pero el atributo `title` del icono de información usa directamente `estimate.description`, que `src/hf_studio/pricing.py:187-196` toma de `pricing_description` en inglés. **Reproducir:** hacer que `/v1/estimate` devuelva una cotización `approx` o `formula` con `description` en inglés; en español, pasar el cursor sobre el icono de información. Falta traducir o presentar esa explicación con una versión localizada. Los mensajes de error recibidos de la API siguen la misma ruta sin localización (`web/src/lib/studio.ts:17-26`), aunque son datos variables y conviene tratarlos por código de error.

6. **[Baja] Quedaron una insignia inglesa y una traducción poco natural en la portada/presets.** `web/src/components/explore/feature-card.tsx:30-39` imprime `NEW` en tarjetas de la portada española, aunque la navegación sí usa «Nuevo» (`web/src/lib/i18n/dictionaries.ts:16`). `web/src/lib/i18n/presets.ts:29-33` traduce *Cinematic establishing shot* como «Plano de establecimiento cinematográfico», un calco; el propio texto descriptivo lo llama «Plano de apertura». **Reproducir:** abrir `/` en español y mirar las tarjetas «Fotograma inicial y final» o «Higgsfield Genjutsu»; luego abrir `/presets/cinematic-establishing`. Faltan «Nuevo» y un término como «Plano de apertura cinematográfico».

## Comprobaciones sin hallazgo

- `pnpm build` y `pnpm lint` terminaron correctamente con Next 16.2.11.
- Los filtros de presets y casos de uso comparan categorías originales y traducen solo la etiqueta. Las opciones de los presets conservan el valor original para `previewPreset` y `runPreset` (`web/src/components/presets/preset-runner.tsx:63-76,170-174,95`); no encontré una opción traducida que se envíe a la API.
- Los ejemplos `?prompt=` conservan el prompt original en inglés y los enlaces mantienen `tab`, `mode` y `model`. `?reuse=` y el envío de `generate` no dependen del idioma. El refresco iniciado por el selector, en la misma pestaña, no aparece en las dependencias de las peticiones ni reinicializa los formularios. No hice una prueba manual de navegador ni una generación de pago.
- Las expresiones de `cost.ts` cubren las formas de `basis` que hoy emite `pricing.py`; las de `workflow.ts` no alteran de forma incorrecta los sufijos de flujo actuales del catálogo. La cobertura futura depende de que esos textos externos no cambien.

## Veredicto

**CAMBIOS REQUERIDOS.** Corregir la búsqueda con términos visibles en español, sincronizar el proveedor con los cambios de idioma fuera de la pestaña y localizar el contenido visible del catálogo. Después, cerrar los restos de presentación y redacción señalados. Hace falta una comprobación manual de cambio de idioma con dos pestañas y formularios abiertos.
